Use Makefile to compile program

The program will create an analysis file with or without 
the processes cycles depending on selection. 

The program will prompt the user to select whether or not to
include processes cycles on analysis or not.